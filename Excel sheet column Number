class Solution {
    public int titleToNumber(String columnTitle) {
        
        int current = 0;

        for(int i = 0;i<columnTitle.length();i++){
            current = current *26;
            current += ((columnTitle.charAt(i)-'A')+1);
        }
        return current;
    }
}
